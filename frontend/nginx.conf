server {
    server_name frontend;
    listen 8000;
    location / {
        root /var/www/html;
        index index.html;
        try_files $uri $uri/ /index.html =404;
    }
    location /nginx_status {
        stub_status;
    }
}
